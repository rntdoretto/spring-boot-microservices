spring:
  application:
    name: auth
  profiles:
    active:
    - local
  cloud:
    discovery:
      enabled: true
      client:
        simple:
          local:
            service-id: configserver