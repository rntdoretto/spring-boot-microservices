############
## SERVER ##
############

server:
  port: ${PORT:8100} 
  

############
## SPRING ##
############

spring:
  application:
    name: currency-conversion-service
  cloud:
    config:      uri:      - ${CONFIG_SERVER_URI:http://localhsot:8888}
  profiles:
    active:
    - ${PROFILES:dev}