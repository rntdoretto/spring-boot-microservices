spring:
  application:
    name: gateway
  profiles:
    active:
    - local
  cloud:
    discovery:
      enabled: true
      client:
        simple:
          local:
            service-id: configserver