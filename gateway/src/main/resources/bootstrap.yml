############
## SERVER ##
############

server:
  port: ${PORT:8765}
  servlet:
    context-path: /microservices
    

############
## SPRING ##
############

spring:
  application:
    name: gateway
  cloud:
    config:
      uri:
      - ${CONFIG_SERVER_URI:http://localhsot:8888}
  profiles:
    active:
    - ${PROFILES:dev}