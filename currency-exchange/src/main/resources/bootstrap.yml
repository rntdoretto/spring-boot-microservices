############
## SERVER ##
############

server:
  port: ${PORT:8000}
  
  
############
## SPRING ##
############

spring:
  application:
    name: currency-exchange-service
  cloud:
    config:
      uri:
      - ${CONFIG_SERVER_URI:http://localhsot:8888}
  profiles:
    active:
    - ${PROFILES:dev}