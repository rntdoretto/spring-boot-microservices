spring:
  application:
    name: course
  profiles:
    active:
    - local
  cloud:
    discovery:
      enabled: true
      client:
        simple:
          local:
            service-id: configserver