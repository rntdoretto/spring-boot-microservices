spring:
  application:
    name: price
  profiles:
    active:
    - local
  cloud:
    discovery:
      enabled: true
      client:
        simple:
          local:
            service-id: configserver